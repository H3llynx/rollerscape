export type UserProfile = {
    id: string;
    name: string;
    avatar_url: string;
    updated_at: string;
}

export type Credentials = {
    name?: string;
    email: string;
    password: string;
}