import { LocationRequest } from "../features/geocoding/components/LocationRequest/LocationRequest";

export function ProfilePage() {
    return (
        <main className="md:grid md:grid-cols-2 w-full">
            <LocationRequest />
            <section>

            </section>
        </main>
    )
}